# build output
/.next/
/out/
.output/

# next
next-env.d.ts
/.vercel

# dependencies
node_modules/

# environment
.env
.env.production
.env*.local

# logs
npm-debug.log*

# os / editor
.DS_Store
.vscode/
*.local

# local tooling
.claude/
.codegraph/

# source documents — local only, never committed. This repo is PUBLIC, so
# committing these would publish teammates' names, matric numbers, and photos
# from the group network report, plus its lab VPN credentials. Site copy is
# written *from* these files; the files themselves stay off GitHub.
docs/reference/

# typescript incremental build info
tsconfig.tsbuildinfo
